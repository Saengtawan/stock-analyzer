# Test environment configuration
QUANDL_API_KEY=

# Alpaca Paper Trading API
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=
FMP_API_KEY=
TIINGO_API_KEY=
DEEPSEEK_API_KEY=

# Database Configuration
DATABASE_URL=

# Flask Configuration
FLASK_ENV=
FLASK_SECRET_KEY=
FLASK_PORT=

# Risk Management Settings
DEFAULT_RISK_PERCENTAGE=
MAX_POSITION_SIZE=
DEFAULT_STOP_LOSS_MULTIPLIER=

# Analysis Settings
DEFAULT_TIMEFRAME=
LOOKBACK_PERIODS=
RSI_PERIOD=
MACD_FAST=
MACD_SLOW=
MACD_SIGNAL=

# Logging
LOG_LEVEL=
LOG_FILE=

# Cache Settings
CACHE_DURATION_MINUTES=
ENABLE_CACHE=
# H12-A deployment (2026-06-07) — paper account
# Multi-model V-2/V-C + cell filter + regime gates + Option E* (Z4)
# H12-B (2026-06-07): H12-A + AD-conditional WIN_THR on Z1/Z3.
#   AD>1.5 -> 0.68 ; AD<0.7 -> 0.80 ; else 0.75. Z2/Z4 fixed 0.75.
#   WF 2yr: +126% (vs H12-A +99%), WR 60%, Sharpe 3.3, 7/9 quarters win.
# Rollback to H12-A: set ML_FILTER_VARIANT=
# Full rollback: comment both lines + restart (or git reset --hard v1.9.0-pre-h12a)
ML_FILTER_VARIANT=
ENTRY_FILTER_SPEC=

# 2026-06-09: disable Z1 gain<=
H12A_Z1_GAIN_CAP=
H12A_THR_Z1=
# 2026-06-10: drop redundant Z1/Z3 sec_rel_strength>0 gates. Ablation: Z1=
# no-op on final picks, Z3=
H12A_DROP_SEC_GATE=
# 2026-06-10: VIX-stress lane — DISABLED. Validated on wf_1min_bars.db which differs
# from gold-standard phase0 labels by ~1.4%/corr0.71 (wrong bar source =
# +47%/Sharpe4.60 SUSPECT. Re-validate on phase0 before re-enabling. Code dormant.
H12A_VIX_LANE=
H12A_VIX_LANE_THR=
# 2026-06-10: MOM-30 — DISABLED. The +0.6-0.9%/trade was a bars-harness artifact.
# On gold-standard phase0 pnl_+30: edge =
# only =
# on phase0 pnl_+30/+60/+90 (already in /tmp/phase0_labels_5yr.pkl), never recompute bars.
H12A_MOM30=
# 2026-06-11: take-profit suggestion in scan output (manual). +3% =
# avoid fade-days (KLAC 06-10). Backtest avg lower than hold-EOD (caps winners). Off: =
H12A_TP_PCT=
# 2026-06-11: spy_intra>0 directional gate on Z1 (market-green-at-entry). DEFAULT OFF
# for A/B. Root cause: pop-fade =
# direction is freshest. Full-WF: Z1 +0.276% -> +0.587% (both holdout folds, 7/9 Q,
# WR 56->62%). Z1 ONLY (Z3 worse, Z2 thin, Z4 has Option E*). PIT-clean. Does NOT replace
# vix<20 (orthogonal: vix=
H12A_SPY_GATE=
# 2026-06-11: drop Z1 vix<20 gate (user-requested live A/B on paper). Backtest says KEEP
# it (vix<20&spy>0 +0.587% vs spy-only +0.268% vs no-gate +0.109%; vix>=
# -0.56%). vix & spy are orthogonal not substitutes. If DROP_VIX=
# SPY_GATE=
H12A_DROP_VIX=
