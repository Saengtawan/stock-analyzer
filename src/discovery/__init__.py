# Discovery Engine — background scanner for high-confidence stock picks
# Display-only, does NOT execute trades
#
# Self-Learning Components:
#   outcome_tracker.py — records predicted vs actual outcomes for every pick
#   calibrator.py — computes rolling accuracy and confidence scores
#   pattern_learner.py — detects feature drift and discovers new patterns
